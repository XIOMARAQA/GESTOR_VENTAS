# Copia a .env.render (NO subir a git). Solo para sincronizar/copiar datos a Render.
# Render → PostgreSQL → Connections → External Database URL
#
# DATABASE_URL=postgresql://gestorventas:TU_CLAVE@dpg-xxxxx.virginia-postgres.render.com/gestorventas?sslmode=require
#
# Copiar BD local → Render:
#   .\scripts\sync_local_to_render.ps1 --yes
