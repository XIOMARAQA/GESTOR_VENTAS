# Copia a .env.render (NO subir a git). Solo para aplicar migraciones a Render desde tu PC.
# Render → PostgreSQL → Connections → External Database URL (+ ?sslmode=require si falla SSL)
#
# DATABASE_URL=postgresql://gestor_ventas_db_user:TU_CLAVE@dpg-xxxxx.oregon-postgres.render.com/gestor_ventas_db?sslmode=require
